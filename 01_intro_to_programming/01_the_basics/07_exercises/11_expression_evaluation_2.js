// What value does the following expression evaluate to?

// parseInt('3.1415')

// This should evaluate to 3 as a numerical value because the parseInt() function coerces a string to a numerical and stops coercing if it encounters a non-integer ('.') and thus stops coercing at 3.

console.log(parseInt('3.1415')); // 3