let firstName = "Plato";
console.log(firstName.toUpperCase()); // PLATO
console.log(firstName); // Plato

/*

.toUpperCase() performs a non-destructive string mutation on the string object. 
.toUpperCase() returns a new string value but leaves the caller unchanged.

*/