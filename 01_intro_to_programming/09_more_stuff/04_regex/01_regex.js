// What is regex?
// Regex is a why of recognizing patterns / sequences of characters within a String.

// The pattern to be found is placed between forward slashes.

// Depending on the method used on the RegEx object the regex pattern will need to be supplied as the object to which the pattern is called on or provided as the argument to the method invocation.